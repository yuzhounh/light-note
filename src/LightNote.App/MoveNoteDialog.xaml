<Window x:Class="LightNote.App.MoveNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="移动笔记"
        Width="420"
        Height="210"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="选择目标笔记本：" FontSize="14" />
        <ComboBox x:Name="DestinationBox" Grid.Row="1" Margin="0,14,0,18" Padding="8,6"
                  DisplayMemberPath="Name" />
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
            <Button MinWidth="78" Content="取消" IsCancel="True" />
            <Button MinWidth="78" Content="移动" IsDefault="True" Click="OnMoveClick"
                    Background="{StaticResource AccentBrush}" Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
