<Window x:Class="LightNote.App.MoveNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="移动笔记"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="22,20,22,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="选择目标笔记本：" FontSize="14" Foreground="{DynamicResource TextBrush}" />
        <ComboBox x:Name="DestinationBox" Grid.Row="1" Margin="0,14,0,18" Height="36"
                  Style="{DynamicResource ToolbarComboBoxStyle}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,4,0,0">
            <Button Content="取消" IsCancel="True" Style="{DynamicResource DialogButtonStyle}" />
            <Button Content="移动" IsDefault="True" Click="OnMoveClick"
                    Style="{DynamicResource DialogPrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
