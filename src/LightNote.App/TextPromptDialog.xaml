<Window x:Class="LightNote.App.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="190"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" FontSize="14" TextWrapping="Wrap" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Margin="0,14,0,18" Padding="10,7"
                 FontSize="14" MaxLength="80" />
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
            <Button MinWidth="78" Content="取消" IsCancel="True" />
            <Button MinWidth="78" Content="确定" IsDefault="True" Click="OnAcceptClick"
                    Background="{StaticResource AccentBrush}" Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
