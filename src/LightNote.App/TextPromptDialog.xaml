<Window x:Class="LightNote.App.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="220"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" FontSize="14" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Margin="0,14,0,18" Padding="10,7"
                 FontSize="14" MaxLength="80" />
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
            <Button Content="取消" IsCancel="True" Style="{StaticResource DialogButtonStyle}" />
            <Button Content="确定" IsDefault="True" Click="OnAcceptClick"
                    Style="{StaticResource DialogPrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
